# 📸 Ajouter l'Image du Logo

## Instructions

1. **Copie ton image de logo** dans ce dossier :
   ```
   apps/web/public/logo.png
   ```

2. **Formats acceptés** :
   - `logo.png` (recommandé)
   - `logo.jpg` ou `logo.jpeg`
   - `logo.svg`
   - `logo.webp`

3. **Le composant Logo** essaiera automatiquement tous ces formats dans l'ordre.

## Emplacement exact

```
AniReserve/
└── apps/
    └── web/
        └── public/
            └── logo.png  ← Place ton image ici
```

## Après avoir ajouté l'image

1. Rafraîchis la page (le logo devrait s'afficher automatiquement)
2. Si l'image ne s'affiche pas, vérifie :
   - Le nom du fichier est exactement `logo.png` (ou un des autres formats)
   - L'image est bien dans `apps/web/public/`
   - Redémarre le serveur si nécessaire

## Taille recommandée

- Largeur : 200-300px
- Hauteur : 80-120px
- Format : PNG avec fond transparent (si possible)






